failure-threshold: warning

ignored:
  - DL3006 # Ignore FROM explicit tags requirement

label-schema:
  author: text
  contact: email
strict-labels: true

override:
  error:
    - DL3003 # usage of cd instead of workdir is an error
    - DL3050 # unknown label is an error